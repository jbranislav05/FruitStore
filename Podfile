# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FruitStore' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FruitStore

pod “Alamofire”
pod ”SwiftyJSON”
pod ”AlamofireImage”
pod ”SDWebImageSwiftUI”

  target 'FruitStoreTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FruitStoreUITests' do
    # Pods for testing
  end

end
